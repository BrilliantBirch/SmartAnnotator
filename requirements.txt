# BrilliantAnnotator 依赖列表
# Python 3.12.10 | 严格锁定版本，禁止自行升级或降级

# GUI 框架
PySide6==6.11.1

# 数值计算与图像处理（版本锁定）
numpy==2.4.6
opencv-python==4.13.0.92
pillow==12.3.0

# 模型推理
# 注意: GPU 推理需安装 onnxruntime-gpu==1.28.0 替代 onnxruntime，详见 requirements-gpu.txt
onnxruntime==1.28.0
onnx==1.19.0

# 配置解析与系统监控
PyYAML==6.0.3
psutil==7.2.2
pynvml==12.0.0

# Windows API（版本锁定）
pywin32==312

# 打包工具
pyinstaller==6.21.0
